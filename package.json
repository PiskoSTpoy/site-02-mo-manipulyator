{
  "name": "app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && node scripts/postbuild.mjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "webp": "node scripts/make-webp.mjs"
  },
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.40",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "sharp": "^0.35.3"
  }
}
